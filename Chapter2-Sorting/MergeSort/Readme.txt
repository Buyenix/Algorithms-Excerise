2.2.6: PlotVisitCount.java, Merge.java and MergeBU.java.
       In this excercise, Merge's count is different with MergeBu's. For example, when size==40, the array partition for these two methods are different.
